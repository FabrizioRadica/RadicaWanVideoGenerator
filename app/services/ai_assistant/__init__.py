"""AI Prompt Assistant service package.

Modular integration (patch §2.4): each concern lives in its own module —
provider drivers, resource management, prompt/plan generation, SequenceQueue
population and configuration persistence — with thin integration layers.
"""
